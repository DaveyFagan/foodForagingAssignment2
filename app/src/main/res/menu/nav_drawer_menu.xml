<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/foragingFragment"
            android:icon="@drawable/ic_add"
            android:title="@string/action_foraging" />
        <item
            android:id="@+id/foragingListFragment"
            android:icon="@drawable/ic_menu_foraging_list"
            android:title="@string/action_foraging_list" />
    </group>

    <item android:title="@string/group_communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/action_share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_mail"
                android:title="@string/action_send" />
        </menu>
    </item>
</menu>